schema_version: 1

cases:
  - id: fixture.removable-skill.shared-cli.deterministic
    product: shared-cli
    domain: fixture
    skill_id: fixture.removable-skill
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: render fixture surfaces
    invocation: verify removable fixture source and rendered surfaces
    expected_exit_code: 0
    expected_artifacts: rendered skill files
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
