# Skill manifest — Plan 01 ships this file empty. Real entries land in
# Plan 03 (`03-reporting-poc`).
#
# Schema: ../core/docs/schemas/skills.schema.json
# Architecture: docs/source/inventory-target-architecture.md `## Manifest Layer`
#
# Each entry MUST declare `id`, `domain`, `source`, `products`, and
# `required_clis`. Phase 1 may temporarily use placeholder values in
# `required_clis`; the audit-drift gate rejects any surviving placeholder
# at the Phase 2 floor.
schema_version: 1
skills: []
